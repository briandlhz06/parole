# Seguridad

Si un puerto público nuevo se le escapa, o el token de Telegram sale en la salida, mail a briandlhz06@proton.me con "parole" en el asunto.

El archivo `~/.parole/baseline.json` no está "protegido": si alguien escribe en tu HOME, ya ganó. Que no avise cuando el server mejoró es a propósito.

Arreglos en `main`.
